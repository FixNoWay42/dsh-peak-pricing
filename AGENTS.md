# 仓库指南

`@deepseek-ai/dsh-peak-pricing` 的开发与协作约定：涵盖项目结构、常用命令、编码规范、开发工作流、强制铁律、测试验证、审计反思、配置安全与完成定义，并在第 13 节并入 Codex 全局规则与全局记忆。本文档同时作为编码代理（如 Codex）的强制指令；标注 **（全局强制）** 的章节对所有人机协作一律生效，其他章节为通用约定。

## 目录

1. [项目概览](#1-项目概览)
2. [开发环境与常用命令](#2-开发环境与常用命令)
3. [编码与测试规范](#3-编码与测试规范)
4. [语言与文案规范（全局强制）](#4-语言与文案规范全局强制)
5. [开发工作流（全局强制）](#5-开发工作流全局强制)
6. [强制开发铁律](#6-强制开发铁律)
7. [测试、验证与闭环（全局强制）](#7-测试验证与闭环全局强制)
8. [缺陷审计与流程制度化（全局强制）](#8-缺陷审计与流程制度化全局强制)
9. [配置与安全（全局强制）](#9-配置与安全全局强制)
10. [文档要求（全局强制）](#10-文档要求全局强制)
11. [完成定义（全局强制）](#11-完成定义全局强制)
12. [工程技能配置](#12-工程技能配置)
13. [Codex 全局规则与记忆（并入）](#13-codex-全局规则与记忆并入)

## 1. 项目概览

### 1.1 系统定位

Cordis 函数插件：在配置的尖峰价格时段内，将 agent 模型请求路由到预设的廉价模型，价格尖峰不命中高成本会话选择。切换是 `agent/request` 瀑布上的按请求实时变换：窗口内用预设 provider/model 替换会话解析结果，窗口外原样放行。

### 1.2 模块边界

| 路径 | 职责 |
|------|------|
| `src/index.ts` | 根插件：`name`/`inject`/`apply`/`Config` 具名导出，`agent/request` 瀑布监听（`prepend`）、配置解析与峰值判定 |
| `src/invariant.ts` | `./invariant` 伴生插件：向 `@deepseek-ai/dsh-invariants` 注册包所有权（无运行时不变式，需在 JSDoc 说明原因） |
| `tests/` | Vitest 规格（`tests/**/*.spec.ts`） |
| `docs/` | 双语文档（英文 + 简体中文），每页配 `*.i18n.yaml` 一致性记录 |
| `examples/` | 可直接挂载的 `cordis.yml` 示例 |
| `scripts/` | 文档门禁：`verify-translation-pairing.mjs`、`verify-md-wrap.mjs` |
| `lib/` | 构建产物（已 gitignore，禁止提交） |

### 1.3 目录约定

- 测试按层放在 `tests/`：单元行为、真实 Loader 组合、伴生插件。
- 文档改动必须同时更新英文与中文页及对应 `*.i18n.yaml`。
- 不得提交 `lib/`、`node_modules/`、`*.tsbuildinfo` 等生成物。

## 2. 开发环境与常用命令

### 2.1 环境要求

Node.js `^22.19 || >=24` 与 pnpm；安装依赖用 pnpm，不用 npm。`pnpm-workspace.yaml` 对 `@deepseek-ai/*@0.1.0-rc.6` 豁免 minimum-release-age，锁文件可即时解析新发布。

### 2.2 常用命令

| 用途 | 命令 |
|------|------|
| 安装依赖 | `pnpm install` |
| 类型检查 | `pnpm run typecheck`（严格 `tsc --noEmit`，无输出） |
| 全量测试 | `pnpm run test`（`vitest run`，单测超时 60 秒） |
| 构建 | `pnpm run build`（`tsc` 产出 `lib/types/`，`tsdown` 打包 `lib/index.js`、`lib/invariant.js`） |
| 文档门禁 | `pnpm run verify:docs`（双语配对 + Markdown 换行） |

### 2.3 发布

发布前必须：`pnpm run typecheck && pnpm run test && pnpm run build && npm publish`（`publishConfig.access: public`）。`exports` 需同步 `.` 与 `./invariant` 两条入口，`files` 仅含 `lib/index.js`、`lib/invariant.js` 与 `lib/types/**/*.d.ts`。

## 3. 编码与测试规范

- TypeScript 严格模式：`strict`、`noUncheckedIndexedAccess`、`exactOptionalPropertyTypes`、`noUnusedLocals`、`noUnusedParameters`。未配置独立 lint/格式化工具，`tsc` 即门禁。
- 插件模块只具名导出（`name`、`inject`、`apply`、`Config`），无默认导出。
- 命名：函数与变量 `camelCase`（如 `isPeakTime`），接口与类型 `PascalCase`（如 `PeakWindow`），常量 `SCREAMING_SNAKE_CASE`（如 `DEFAULT_PEAK_WINDOWS`）。
- 每个公开导出写 JSDoc（模块级注释、`@param`、`@returns`、契约说明，如 `[start, end)` 窗口语义与 `reasoningEffort` 替换规则）。
- 配置错误在加载时抛出以 `peak-pricing:` 为前缀的报错，绝不静默跳过。
- 源码导入使用 `.ts` 后缀（`allowImportingTsExtensions` + `rewriteRelativeImportExtensions`）。

## 4. 语言与文案规范（全局强制）

- 注释与 commit message 一律使用中文；代码标识符、API 字段与内部枚举使用稳定英文机器值。
- 本仓库文档为中英双语：每页文档必须成对维护（英文 + 简体中文 + `*.i18n.yaml` 一致性记录），改一页必须同步另一页，提交前运行 `pnpm run verify:docs`。

## 5. 开发工作流（全局强制）

### 5.1 规则适用优先级

安全和授权优先；未经授权不得暴露凭证、修改生产数据或产生真实外部副作用，默认使用隔离环境、只读接口或 dry-run。沿目标路径读取所有适用的更具体指令；同层冲突时采用更严格的安全、验证和数据完整性要求，并记录决策。规则模糊时按“更安全、更可验证、更可回滚”解释，并在交付中说明假设。

### 5.2 Worktree 隔离（必须使用独立 worktree）

本仓库所有开发、编辑、提交与推送必须使用独立 git worktree，禁止在主检出目录（`main` 的普通 checkout）直接编辑、提交或推送：

1. 进入项目执行任务前必须运行 `~/.codex/scripts/check_worktree_isolation.py --path <项目目录>` 作为强制门禁：退出码 0 才允许直接开工；退出码 1 表示处于主检出目录或同一 worktree 存在其他活跃 Codex 会话，必须先 `git worktree add` 创建独立工作区或等待并发会话结束；非 git 目录输出 NA 且退出码 0，不适用本规则。
2. 每个 Codex 实例只在自己分配的 worktree 中读写文件，禁止多个 Codex 实例同时在同一 checkout 内直接修改文件；提交与推送在各自 worktree 的分支上进行，避免互相覆盖。
3. 共享构建产物、依赖与测试服务的验证门禁在各自 worktree 内执行，或使用仓库级原子锁；禁止并发实例共享 `node_modules`、生成目录或测试数据库。
4. 并发/脏工作区仍按明确文件清单暂存，禁止 `git add -A`；worktree 间冲突先解决再合并，不得覆盖共享文件。
5. 例外仅限仓库初始化、worktree 创建本身或用户明确指定。

### 5.3 Issue 与分支

- 非琐碎变更先建 Issue，并在提交和 PR 中使用 `Fixes #N` 关联；重大架构、数据迁移或职责调整先记录 ADR。一行注释或 typo 可免建 Issue，但提交仍需说明。
- 工单标题不得为占位词（skip/跳过/s/pending 等），正文必须带「## 验收标准」章节与可勾选项；关闭工单前逐条核对并勾选验收项，未满足不得关闭。
- 禁止直接在 `main` 开发；从 `main` 创建分支，命名规范：`feat/#N-xxx`、`fix/#N-xxx`、`docs/xxx`、`refactor/xxx`、`chore/xxx`、`perf/xxx`。
- **PR 头部分支名禁止含 `#`**（Gitea 对含 `#` 分支名的合并接口返回 405）：PR 分支用 `feat/N-xxx`、`docs/N-xxx`，Issue 关联写在提交/PR 描述中的 `Fixes #N`。

### 5.4 提交规范

- 使用聚焦的 Conventional Commit 提交主题，通常使用中文，例如 `feat(peak-pricing): ...`、`fix: ...`、`test: ...`、`docs: ...`。
- 提交信息仅可使用前缀：`feat`、`fix`、`refactor`、`docs`、`chore`、`test`、`style`、`build`、`ci`、`perf`；禁止 `update`、`index` 等非标准前缀。
- 提交正文写清改动、原因、影响范围和数据流变化，不得使用 `Co-Authored-By`、`Generated-by` 等 AI trailer。
- 账号约定（不改默认 git 配置）：AI agent 提交必须显式署名 `ByteOrigin`，每次 commit 使用 `git -c user.name="ByteOrigin" -c user.email="ByteOrigin@liuyuesong.asia" commit ...`（author/committer 同时生效，仅本次命令有效，不写任何配置文件）；人工（liuyuesong）提交保持默认配置不变；`contract-guardian-bot` 账号仅用于机器人自动操作，不得用于人工代码提交。
- 仅暂存明确路径，禁止 `git add -A`、`git add .` 和通配批量暂存；默认逐文件提交，提交后运行 `git show --stat --oneline HEAD` 核对文件数。
- 提交前运行 `pnpm run typecheck` 与聚焦测试（本次改动涉及的测试），文档改动另须 `pnpm run verify:docs`；验证与 commit/push 必须通过 `&&` 等成功条件连接，不能用单纯换行或 `;`。
- 提交前确认密钥、token、运行日志、数据库和真实生产配置未被跟踪；不得回滚或覆盖他人改动。
- 禁止向 `main` force-push、跳过 Git hook 或修改 Git 配置规避规则。

### 5.5 PR 流程

- 非 `main` 分支推送后使用平台 API 创建 PR，采用 squash 合并。
- PR 必须包含行为变化、验证命令（typecheck/test/build/verify:docs）、文档更新清单和 Issue 链接（`Fixes #N`）；高风险变更还要包含归因/反思文档和 ADR；涉及界面时附截图。
- 合并 PR 后核对合并结果：分支上新推送的提交是否进入 main，发现缺口须追回（新 PR 或确认丢弃），不得静默丢失。

### 5.6 编辑与实施协议

- 编辑前，从仓库根沿目标路径读取所有适用指令，检查现有代码、测试、配置和 Git 状态，并保护与任务无关的用户改动。
- 实施时优先复用既有模式、API、Schema 和测试工具，避免不必要的新抽象；结构化数据使用结构化解析器或 API，并在边界校验输入。注释只解释不显而易见的推理。
- 任何来源都不得写入真实 API 密钥、密码、令牌、私钥或生产 URL。
- 自主选择风险可控、可验证、可回滚的方案并直接实施，不把普通技术选型、实现路径、文件组织或测试策略交给用户逐步确认。
- 模块边界、配置结构、职责划分、数据迁移或批量流程等重大架构决策，先在 ADR 中写明背景、备选方案、决策和后果。
- 完成代码后必须进行独立审查，不能只依赖自测。

## 6. 强制开发铁律

### 6.1 项目铁律（必须执行）

1. 先写 `tests/*.spec.ts`，再实现功能；每个行为变更都必须有失败测试（RED）→ 通过（GREEN）。
2. 每写一个文件就提交一次，确保变更可追溯。
3. 替换时同提交删除被替代代码，或显式退役（删除实现 + 同步文档）；禁止「只增不删」留死代码。
4. 新代码必须覆盖被替代代码的全部能力。

### 6.2 全局铁律（同样强制执行）

更深层规则优先，冲突时采用更严格的安全、验证和数据完整性要求：

1. 注释、文档和提交信息一律使用中文；代码标识符可保留稳定英文。
2. 非琐碎变更先建 Issue，并在提交和 PR 中使用 `Fixes #N` 关联；重大架构、数据迁移或职责调整先记录 ADR。
3. 所有行为变更按 TDD 执行：先保留 RED 失败证据，再实现 GREEN、回归和重构；`fix:` 提交还必须记录根因、漏检原因、反证结果和防复发机制。
4. 完成实现后进行独立审查，并以新鲜的测试、静态检查和运行证据加主动证伪作为完成依据；不允许用预览、metadata、fixture 或缩小范围伪装真实集成。
5. 禁止以 mock、录制回放或伪造响应替代真实行为。外部依赖使用隔离环境、真实本地服务、只读接口或 dry-run；未授权不得触碰生产数据、凭证或产生不可逆副作用。
6. 密钥、令牌、密码、私钥、真实生产 URL、运行日志和数据库不得入库；删除、覆盖或迁移真实数据前须有备份、回滚方案和明确授权。
7. 编辑前读取适用指令、检查代码/测试/配置与 Git 状态；复用既有模式，限制改动范围，并保留无关工作区变更。
8. 默认从 `main` 创建 `feat/#N-xxx`、`fix/#N-xxx`、`docs/xxx` 等分支（**PR 头部分支名禁止含 `#`**，见 5.3）；禁止 `git add -A`、`git add .`、跳过 hook 或向 `main` force-push。逐文件暂存、验证、提交和推送，并用 `git show --stat --oneline HEAD` 核对提交范围。
9. 代码、契约、CLI、API、配置、部署和安全边界变更必须同步更新对应文档；重大决策写入 `docs/adr/` 或项目指定 ADR 目录。
10. 已授权任务只能以 `TASK_COMPLETE` 或客观硬阻塞 `TASK_BLOCKED` 结束。完成前检查输入、处理、回写、审计、可观测性与失败恢复的闭环，并记录可推翻条件、反证结果和残余风险。

## 7. 测试、验证与闭环（全局强制）

### 7.1 TDD 与真实行为要求

- 行为变更遵循 TDD：RED → GREEN → 重构。
- 全局禁止以 mock、`vi.mock`、Playwright route mock、录制响应回放、伪造客户端响应或其他替身冒充真实行为；本仓库的 Loader 组合测试使用真实 `@deepseek-ai/cordis-plugin-loader` 与 `@deepseek-ai/cordis-plugin-include` 挂载，不使用替身。
- fixture 仅可提供不替代行为的静态输入，不得替代 API、数据库、消息总线、文件系统、浏览器或外部服务。
- 外部系统使用隔离环境、真实本地服务、容器化依赖、只读接口或 dry-run；依赖不可用时明确标记验证受阻，不得用 mock 制造通过证据。

### 7.2 验证范围与前端验收

- 先运行最小相关测试、lint 和类型检查，再按风险扩大验证。失败、过期生成物、缺失链接和未解决 TODO 都是未完成工作。
- 完成前至少验证：`pnpm run typecheck`、`pnpm run test`、`pnpm run build`、文档改动时的 `pnpm run verify:docs`，以及从输入到结果回写和审计记录的示例闭环；不能只以退出码为零作为结论，必须从用户视角验证实际结果。
- 涉及前端时（本仓库当前无前端，若引入子项目则适用），对运行中的同源门户进行桌面和移动视口浏览器验收，检查 Console、Network、pageerror 和至少一个相关真实交互；单元测试、API 测试、curl、构建、metadata、preview 或静态截图不能替代。

### 7.3 变更闭环检查

- 每次变更完成后，检查与现有功能的完整闭环：上游入口和触发、权限与审批、核心处理、跨模块调用和事件契约、状态与结果回写、调用方展示、审计、通知与可观测性、失败恢复与重试、兼容入口和唯一事实来源。
- 识别生产者、消费者、共享模型、状态机、配置、API、事件、任务、报表和运维入口；发现数据孤岛、状态不同步、契约漂移、重复事实来源或断链时，在同一任务修复并复验。
- 功能、修复和重构完成后执行全景多维审视（小改动可豁免），改进项创建 Issue。
- 每个“已闭环”结论均须写出可推翻条件；无法验证关键下游或反证未解决时，任务不得结束。

## 8. 缺陷审计与流程制度化（全局强制）

### 8.1 `fix:` 提交前置审计

- `fix:` 提交前必须完成审计反思，不能把“测试通过”直接视为修复完成。
- 保留 RED 失败证据，实施最小修复取得 GREEN 和完整回归；记录症状、影响、复现条件、根因、错误数据或控制流来源、引入和漏检原因、可推翻条件、主动反证、完整验证和防复发措施。
- 反思写入 `docs/retrospectives/` 或仓库指定归因目录；同一问题追加既有记录。
- `fix:` 正文说明根因、防复发和验证范围；根因不明、不能构造失败测试或反证未收敛时不得创建 `fix:`。
- 纯格式使用 `style:`，测试契约使用 `test:`，文档使用 `docs:`。

### 8.2 流程制度化

- 审计或反思发现的流程问题，必须在同一任务制度化防复发措施；只有 prose、建议或 Checklist 不算完成。
- 优先级固定为：状态机/Schema 门禁 > 自动测试/CI/Git hook > 静态扫描 > 运行监控告警 > 带客观证据字段的强制 Checklist > 文档。
- 能在仓库自动化的措施必须实现为代码、测试、脚本、hook、CI 或校验器，并用旧失败场景证明其会拒绝。
- 每项记录失败模式、强制执行点、机制、证伪测试、适用范围、触发时机、责任主体和已知绕过；完成审计输出“流程制度化矩阵”，列出问题、执行点、机制文件或命令、证伪证据和残余风险。

## 9. 配置与安全（全局强制）

### 9.1 配置

- 插件配置由 schemastery `Config` schema 定义并在加载时校验；未知时区、非法 `HH:mm`、`start >= end`、空窗口、不可解析的 `effectiveFrom`、缺失 `peak.provider`/`peak.model` 都必须在挂载时抛错。
- 绝不提交 `.env`、`config/secrets.toml` 或任何真实密钥文件；配置文件模板（如有）以 `*.example` 形式提供。
- 会产生外部副作用的脚本优先使用 `--dry-run` 或 `--once`。

### 9.2 安全边界

- 禁止提交 `.env`、token、API key、密码、私钥、真实生产 URL、包含敏感字段的数据库或日志。
- 未经授权不得发送生产告警、重启主机、创建/销毁虚机、同步镜像或执行 Playbook。
- 未启用、未审批或未命中精确白名单的外部效果，状态机必须返回 `disabled` 或 `external_blocked`，不能把未外发、preview、metadata 或模拟结果记录为成功。
- 删除、覆盖或迁移真实数据前必须提供备份与回滚方案并取得明确授权；外部集成默认最小权限且可审计。

## 10. 文档要求（全局强制）

- 代码变更必须同步更新文档：公开行为更新契约文档（`docs/usage.md`、`docs/architecture.md` 等），配置更新配置文档，发布流程更新 `docs/development.md`，安全边界更新 SECURITY 文档。
- 每页文档必须中英文配对并同步 `*.i18n.yaml`，提交前运行 `pnpm run verify:docs`；新增模块或功能须有配套中英文文档。
- README 作为入口提供功能清单、快速开始、架构概览和完整索引；重大决策写入 `docs/adr/` 或指定 ADR 目录，并在 PR 中列出文档更新。

## 11. 完成定义（全局强制）

- 已授权任务只允许以 `TASK_COMPLETE` 或客观硬阻塞 `TASK_BLOCKED` 结束。
- 不得把阶段性实现、部分测试、可重试工具错误、超时、上下文压缩、工作量、审计发现或待办当作终态。
- `TASK_COMPLETE` 需要完成定义、空未完成清单、新鲜的强制验证和主动证伪、预期 commit/push，以及输入、处理、执行、验证、回写、审计和失败恢复的闭环证据。
- `TASK_BLOCKED` 必须说明客观硬阻塞和解除它的唯一最小条件。
- 禁止承诺“继续处理”后停止调用工具，也不得以缩小范围、降低验收、metadata/preview 或忽略失败证据提前结束。

## 12. 工程技能配置

本仓库无自建工单/PR 自动化脚本；Issue 与 PR 按托管平台（Gitea/GitHub）标准流程管理，遵循第 5.3、5.5 节约定。单上下文仓库可使用根目录 `CONTEXT.md` 与 `docs/adr/` 维护领域文档；缺失时静默继续。

## 13. Codex 全局规则与记忆（并入）

以下内容并入自 Codex 全局配置（`~/.codex/AGENTS.md`）与全局记忆（`~/.codex/memories/memory_summary.md`），确保任何在本仓库工作的代理都能获得与 Codex 相同的全局规则与记忆。与上文既有章节重复的规则以说明衔接，不重复堆叠。

### 13.1 全局自主开发约定

- 对已明确授权持续推进的开发任务，编码代理应自动选择风险可控、兼容性最好且可验证的推荐方案，不逐步请求用户确认。
- 除非涉及不可逆操作、生产数据风险、真实生产凭证、外部审批、人工业务判断或强制规范冲突，否则持续执行分析、设计、编码、测试、文档、提交和推送。
- 功能实现后必须自动执行完成审计和反思；若审计或反思发现本地可修复问题，应继续采用 TDD 自动修复、重新验证、更新文档并提交推送，直到目标真正完成。
- 每次任务完成后，本地可修复的残余风险必须在该任务内自动解决并复验，禁止以“已知残余风险”“后续处理”“下阶段再做”等表述遗留到完成态；仅当残余风险源于真实硬边界（不可逆操作、生产数据风险、真实生产凭证、外部审批、人工业务判断、未经授权的真实外部副作用）时允许登记，且必须写明解除条件、验证方式与责任主体。
- 不得通过缩小范围、放宽验收口径、把 metadata/preview 冒充真实集成或忽略失败证据来宣称完成。
- 审计与反思必须执行证伪：对每个关键完成结论声明可推翻它的具体条件，记录直接证据、主动反证搜索、发现的反证、结论状态与残余风险。不得只收集支持性证据；发现反证时先判定结论被推翻，继续自动修复并复验，反证未解决前不得宣称完成。
- 涉及外部系统时默认使用 mock、fixture、隔离测试环境、只读接口或 dry-run；未经明确生产授权不得产生真实外部副作用。

### 13.2 技能自动匹配与调用门禁

- 每个任务开始规划前，必须先对照本会话 `Available skills` 列表（技能名 + description + 触发词），判断是否存在覆盖当前请求的技能。
- 有匹配技能 → 必须显式读取该技能 `SKILL.md` 并按流程执行，禁止用手动等价操作（如自行 `rg` 检索、自写流程）替代；同一任务可组合多个技能。
- 无匹配技能 → 才允许使用通用能力；需要从外部技能生态获取新技能时，调用 `find-skills` 技能（`npx skills find`），不得只凭本机目录手动遍历。
- 完成汇报必须声明本任务使用的技能；存在匹配技能但未使用时，必须说明理由。
- 审计与证伪：`~/.codex/scripts/audit_skill_invocations.py` 扫描会话日志，检测“显式触发词已出现但对应技能未被读取”的缺口（退出码 1 = 有违规）；完成审计、`fix:` 提交门禁与每周复盘时应运行并记录结果。
- 新技能安装同步门禁：每次新安装技能后，必须在「技能调用登记表」登记（名称/触发词/调用场景/路径），并运行 `~/.codex/scripts/audit_skill_invocations.py --check-agents-sync` 验证；登记与审计通过前不得宣称安装完成。
- 已知残余风险：触发词过于宽泛（如 `zabbix`/`webhook`）会产生误报，以人工判断为准，不得据此放宽调用要求。

### 13.3 技能调用登记表（新安装技能必须登记）

| 技能 | 触发词 | 调用场景 | 路径 |
|------|--------|---------|------|
| `multi-project-merge-methodology` | 合并项目、项目合并、系统整合、多项目统一 | 合并两个及以上项目/代码库时（盘点→迁移→验证） | `~/.codex/skills/multi-project-merge-methodology/SKILL.md` |
| `fullstack-jtbd-delivery` | 全栈交付、JTBD、逆向分析、功能梳理、四阶段交付、真实浏览器验收 | 对既有/新项目做四阶段全栈交付（JTBD 逆向→PRD→实现→真实浏览器验收） | `~/.codex/skills/fullstack-jtbd-delivery/SKILL.md` |
| `pr-two-axis-review` | 双轴审查、标准轴、规范轴、代码审查、PR 审查、需求符合性 | 对功能分支 PR 做标准轴+规范轴并行独立审查并合并报告 | `~/.codex/skills/pr-two-axis-review/SKILL.md` |
| `feature-migration` | 功能迁移、迁移过来、移植功能、参考 X 项目把 Y 功能、合并功能到本项目 | 把单个功能/模块从源项目迁移到目标项目（盘点→移植→适配→验收） | `~/.codex/skills/feature-migration/SKILL.md` |
| `system-audit` | 系统审查、系统审计、架构审查、四维审查、系统全景审查、代码质量审计、数据治理审查、业务流程审查、审查范围与前提、根因与整改路线图、闭环验证检查清单、系统材料审查 | 对软件系统材料做架构/代码/数据/业务流程四维审查并输出可验证整改报告 | `~/.codex/skills/system-audit/SKILL.md` |
| `product-strategy-session` | 产品经理、全局设计、大型任务开始、整体方案、策略设计、方案探索、路线图设计 | 大型任务开始时先由产品经理对全局做设计（定位→问题框定→方案探索→路线图），再进入实施 | `~/.agents/skills/product-strategy-session/SKILL.md` |
| `prd-development` | 产品经理、PRD、需求文档、产品需求、全局设计产出 | 产品经理全局设计后产出结构化 PRD（问题→用户→方案→指标→故事）作为实施输入 | `~/.agents/skills/prd-development/SKILL.md` |
| `product-manager` | 产品、产品经理、产品方案、产品设计、新功能设计、功能扩展、功能融合、存量系统加功能 | 以自包含提示词扮演资深产品经理（8 年以上 App 经验），对已上线运行的存量系统做新功能全局设计：信息采集→双融合策略→十阶段正式设计（需求/用户故事/功能结构/交互/状态机/融合性设计/业务闭环/孤岛排查/数据字典/埋点回流/研发落地拆解/两张 Mermaid 图）→至少 3 轮反思；提示词内嵌于 SKILL.md，不依赖外部 agent 目录 | `~/.agents/skills/product-manager/SKILL.md` |
| `issue-rewriter` | 工单重写、重写工单、重写 #N、#N 重写、N 工单重写、N 重写、重写工单 N | 用户以「工单号 + 重写」指令要求自动重写指定 Gitea 工单（按工单 #382 规范结构化、保留原正文、标签闭环 needs-rewrite→needs-triage+issue-rewritten、幂等） | `~/.codex/skills/issue-rewriter/SKILL.md` |

登记约定：新安装技能后必须在本表新增一行并运行 `audit_skill_invocations.py --check-agents-sync` 通过；登记与审计通过前不得宣称安装完成。

### 13.4 非终态禁止 Idle 门禁（最高优先级）

- 用户明确说“继续完成剩余所有任务直到全部完成”或语义等价表述时，必须将整体剩余清单视为一个持续目标；单个功能、单个阶段、单次 commit/push 或一轮验收通过都只是中间进度，不得作为结束回合、转入 idle 或要求用户再次输入“继续”的理由。
- 已明确授权持续推进的任务只有 `TASK_COMPLETE` 和 `TASK_BLOCKED` 两种合法终态；进入合法终态前，编码代理不得结束执行、输出普通 final 后进入 idle，或等待用户发送“继续”“继续任务”等续跑指令。
- `TASK_COMPLETE` 仅在任务完成定义全部满足、未完成清单为空、强制验证与主动证伪具有新鲜直接证据、预期 commit 与 push 均完成后允许使用；最终回复必须以 `TASK_COMPLETE` 开头。
- `TASK_BLOCKED` 仅限不可逆或生产数据风险、真实生产凭证、外部审批、人工业务判断、未经授权的真实外部副作用或无法调和的强制规范冲突；必须先完成所有不依赖阻塞的安全工作，最终回复以 `TASK_BLOCKED` 开头并给出客观证据和解除阻塞所需的唯一最小条件。
- 阶段性实现、部分测试通过、测试仍非全绿、存在剩余断链或待办、工具可重试错误、命令超时、上下文压缩、工作量较大以及审计发现新问题，均不是合法终态。
- 存在本地可修复的残余风险或遗留“已知问题”未解决时，同样不是合法终态；必须继续自动修复、复验并更新文档，直到残余风险被消除或确认为真实硬边界并登记解除条件。
- “尚未完成”“仍未完成”“还不是全绿”“剩余断链”“剩余问题”“下一步”“后续还要”“我会继续处理”及语义等价表达均是明确非终态信号。任何输出一旦包含这些信号，本回合就不得结束，必须立即继续推进。
- 禁止在输出“我会继续处理”“接下来继续”等承诺后停止调用工具。阶段性总结只能作为 commentary/progress update，不得作为 final response。
- 每次准备结束回合前必须检查未完成清单、强制验证、审计与证伪、文档、commit 和 push、合法硬阻塞；任何应完成项未满足且不存在合法硬阻塞时，唯一允许的动作是继续工作。
- 上下文压缩、会话恢复或工具中断后，必须根据持久 Goal、计划文件、Git 状态、测试记录与现有产物恢复未完成清单并继续，禁止要求用户重复任务或手工触发普通续跑。
- 长任务优先使用 Codex `/goal`；存在项目 Watchdog 时，应由 Watchdog 检测非终态最终消息并调用 `codex exec resume` 续接原 session。Watchdog 达到续跑上限属于未收敛失败，不得伪装成任务完成或业务硬阻塞。

### 13.5 `fix:` 提交全局审计反思门禁

- 在任何仓库创建 `fix:` commit 前，必须显式调用并完整执行 `continuous-audit-reflection` 技能。
- 必须用 TDD 复现 bug，保留 RED 失败证据，再实施最小修复并取得 GREEN 与完整回归证据。
- 必须反思并记录：症状与影响、根因、bug 如何被引入、原测试或审查为何漏检、可推翻修复结论的条件、主动证伪结果及防复发措施。
- 仓库存在 `docs/retrospectives/` 时将记录写入该目录；否则写入仓库约定的审计/复盘文档位置。已有相关记录时追加，不创建数据孤岛。
- 根因不明、没有失败测试、证伪未收敛或回归未通过时，禁止创建 `fix:` commit。
- commit 正文必须说明根因、防复发措施和验证范围；不得用 `fix:` 包装格式、测试或纯文档变化来规避门禁。

### 13.6 反思流程制度化全局门禁

- 每次审计或反思产生的流程层发现，必须转化为强制、可执行、可证伪的防复发机制；复盘文档本身不等于整改完成。
- 每个流程发现必须包含：失败模式、执行点、机制、证伪测试、适用范围、触发时机、责任主体、已知绕过与残余风险。
- 控制优先级固定为：状态机/Schema 门禁 > 自动测试/CI/Git hook > 静态扫描 > 运行监控告警 > 强制 Checklist > 文档。
- 只要仓库内可以自动化，就必须在同一任务实现并验证，不得仅提出建议；无法自动化时必须写明真实外部边界和客观证据要求。
- 仓库规则同步写入仓库 `AGENTS.md`；跨仓库复用规则同步维护适用 skill。
- 完成报告必须提供流程制度化矩阵。任何本地可执行措施仍只停留在 prose 时，不得宣称完成。
- 并发或脏工作区中默认按明确文件清单暂存；只有确认全部变更归属当前任务且敏感/运行文件扫描通过时，才允许 `git add -A`。
- 将验证与 commit/push 放在同一 shell 调用时，必须使用 `&&` 或等价的显式成功条件串联；禁止仅用换行或 `;`，避免测试失败后仍继续提交。
- 会重装依赖、生成资产、构建或启动共享测试服务的全量门禁必须使用仓库级原子锁；同一 checkout 已有实例运行时必须在写入前退出，禁止并发门禁共享 `node_modules`、生成目录或测试数据库。

### 13.7 并发 Worktree 隔离门禁

与第 5.2 节「Worktree 隔离」为同一门禁，内容已并入该节，此处不再重复，以 5.2 为准。

### 13.8 Obsidian 跨项目记忆门禁

- 每个非琐碎任务开始前，按任务关键词读取 `/Users/liuyuesong/knowledge-base/00-系统/项目总览.md`、`40-偏好与决策/INDEX.md` 和对应项目页；只读任务相关笔记及最近相关周报，避免全库冗余读取。
- 将本次已验证、可跨任务复用的偏好、决策、项目结论以脱敏摘要同步到知识库；不得复制密码、令牌、Cookie、私钥、完整连接串或原始会话。
- 知识库历史不是当前事实证明。与当次用户指令、仓库 `AGENTS.md` 或当前代码冲突时，按当次指令、仓库规则、当前可验证事实、历史笔记的顺序处理。

### 13.9 全局记忆与用户偏好（来自 `~/.codex/memories/memory_summary.md`）

**用户画像**：用户主要从事运维自动化开发（OpsEye 为主，OpenSRE 相关）；要求端到端、有证据支撑的交付，不接受范围缩小的绿测或仅设计声明。用户常授权自主实施，期望代理在普通失败中继续推进，仅在具体硬阻塞或确有风险的外部副作用时暂停；重视共享脏工作区中的干净文件边界、经授权的 `main` 直接提交/推送、中文沟通，以及如实汇报本地/隔离/外部验证分别证明了什么。

**协作偏好**：
- 未完成的长任务必须持续推进直到“完整的任务结束”或有证据支撑的 `TASK_BLOCKED`；不得在“我会继续处理”后 idle 或等待再次“继续”。
- 不得把 preview、metadata、mock、隔离 fixture 或狭窄测试当作完成证据；需验证真实用户旅程，外部前置条件标记为 `external_blocked`。
- 保留与任务无关的脏工作区改动；显式暂存文件、避免 `git add -A`；非 fast-forward 更新用 rebase 而非 force-push。
- 前端工作必须用桌面/移动证据验证真实线上路由；build/test/hash 等价不足以证明。
- 不可逆/高风险外部操作前先询问；凭证创建忽略占位符，绝不在聊天中索要密钥。
- 中文协作；经请求时直接在 `main` 工作并推送。
- 数据路径硬约束：不做 Loki 侧日志排除；绝不把多条日志合并进一次 L1 AI 请求。

**通用要点**：
- 全局 Codex 监督下只有 `TASK_COMPLETE` 或硬证据 `TASK_BLOCKED` 是终态；Hook 信任需要精确 SHA-256 状态与 `mergeStrategy: "upsert"`。
- 链接 checkout 缺少设计文档时，先查父目录与 `.worktrees` 再假设规范缺失。
- 全局 `/Users/liuyuesong/.codex/skills/functional-audit` 是“方案完成审计/独立验证”的入口：既有测试能定位代码，但不是验收证据。

**项目记忆索引**：OpsEye、OpenSRE 等其它仓库的逐仓库记忆（上线结论、设计文档、验收口径）保存在 `~/.codex/memories/`（`memory_summary.md` 与 `rollout_summaries/`）；在本仓库工作时按需查阅本仓库相关条目，涉及其它仓库时先查对应记忆。

### 13.10 Codebase 知识图谱（codebase-memory-mcp）

若当前环境提供 codebase-memory-mcp 工具，代码发现优先使用图谱工具而非 grep/glob/文件搜索：

1. `search_graph`：按模式查找函数、类、路由、变量
2. `trace_path`：追踪调用链（入向/出向）
3. `get_code_snippet`：读取具体函数/类源码
4. `query_graph`：复杂模式的 Cypher 查询
5. `get_architecture`：项目高层结构摘要

仅在以下情况回退到 grep/glob：搜索字符串字面量、错误消息、配置值；搜索非代码文件（Dockerfile、shell 脚本、配置）；MCP 工具结果不足时。
